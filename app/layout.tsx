import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "BusinessIQ - Professional Business Intelligence Platform",
  description:
    "Advanced AI-powered business analytics, market intelligence, and content generation for modern enterprises",
  keywords:
    "business intelligence, AI analytics, market research, SEO optimization, business insights, professional dashboard",
  generator: "v0.dev",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
